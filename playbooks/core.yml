---
- name: Configurar Core MikroTik
  hosts: core
  gather_facts: no

  tasks:
    - name: Aplicar config base do Core a partir de ficheiro .rsc
      community.routeros.command:
        commands: "{{ lookup('file', '../files/core-config.rsc').splitlines() }}"
