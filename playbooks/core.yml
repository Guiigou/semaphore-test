---
- name: Configurar Core MikroTik
  hosts: core
  gather_facts: no
  connection: network_cli

  tasks:
    - name: Aplicar config base do Core a partir de ficheiro .rsc
      community.routeros.routeros_config:
        src: files/core-config.rsc
